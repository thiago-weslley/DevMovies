import { styled } from "styled-components";

export const Container = styled.div`
  padding: 3rem 2rem 0 2rem;
  overflow-x: hidden;

  h2 {
    margin-top: 6rem;
    margin-bottom: 4rem;
  }

  .swiper-wrapper {
    display: flex;
  }
`;
